import React from 'react';
import './ProfileSkillChip.css';

const ProfileSkillChip = ({ children }) => (
    <span className="profile-skill-chip">{children}</span>
);

export default ProfileSkillChip;
